{
  "name": "quiz-gen-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @carter-apas/quiz-gen",
    "build": "npm run build -w @carter-apas/quiz-gen",
    "start": "npm run start -w @carter-apas/quiz-gen",
    "test": "npm run test -w @carter-apas/quiz-gen",
    "lint": "npm run lint -w @carter-apas/quiz-gen",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
